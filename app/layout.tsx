import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Rossel - Media Planning',
  description: 'Application de planification média pour le groupe Rossel',
  keywords: 'media planning, Rossel, advertising, campaigns, Le Soir, DH, Sudinfo',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="fr">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}